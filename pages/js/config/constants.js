export let OBRIGATORIO = (campo) => {return campo + ' é obrigatório.';}

export let SUCESSO_CADASTRAR = (campo) => {campo +' cadastrada com sucesso.';}
export let SUCESSO_EXCLUIR = (campo) => {campo +' excluido com sucesso.';}

// export let CONFIRM_CANCELAR = 'Deseja cancelar?';

// export let LOCATION_REF_PAGINA = '../pagina-do-voluntario/pagina-do-voluntario.html';

export const LOCAL_JSON_SERVER_URL = 'http://localhost:3001';
export const LOCAL_SISTEMA = "http://localhost:3003";
